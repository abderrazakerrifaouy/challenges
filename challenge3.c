#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
   int x , y ;
   printf(" enter the value  a ; ");
   scanf("%d",&x);
   printf(" enter the value b ");
   scanf("%d",&y);
    printf("a + b is %d", x + y);
    printf("\na - b is %d", x - y);
    printf("\na * b is %d", x * y);
    printf("\na / b is %d", x / y);
    printf("\na % b is %d", x % y);
}
